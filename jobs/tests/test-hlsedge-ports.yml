- name: Check if port 8080 listening
  wait_for:
    port: "{{ port.two }}"
    delay: 5
    timeout: 10

- name: Check if port 8081 is listening
  wait_for:
    port: "{{ port.six }}"
    delay: 5
    timeout: 10