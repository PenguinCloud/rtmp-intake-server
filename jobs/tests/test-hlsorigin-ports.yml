- name: Check if port 1935 are listening
  wait_for:
    port: "{{ server.portone }}"
    delay: 5
    timeout: 10

- name: Check if port 8080 is listening
  wait_for:
    port: "{{ server.porttwo }}"
    delay: 5
    timeout: 10